# Pixel Transformer + CIL

Same Transformer architecture and CIL training trick as
``latent_tx_cil``, but operates directly on ``32^3`` voxel patches
without any latent-space compression. Useful as a "no compressor"
sanity check.
